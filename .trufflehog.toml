# TruffleHog Configuration
# Excludes false positives from secret scanning

[allowlist]
# Exclude node_modules (contains test data with JWT examples from dependencies)
# and documentation files
paths = [
    "node_modules/",
    "docs/ENV_SETUP.md",
    "docs/DEPLOYMENT.md",
]
